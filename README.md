# File-Search-Tool
🧠 Project Info  A Python script that searches for a file by name inside a folder (including subfolders).  It:  Takes folder path Takes file name Searches recursively Shows full path if found  Very practical — like a mini file search utility 🔍
